rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default: deny all access
    // API handles uploads via Firebase Admin SDK (bypasses these rules)
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
