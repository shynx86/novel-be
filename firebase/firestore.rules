rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all access
    // API handles auth via Firebase Admin SDK (bypasses these rules)
    // These rules protect direct client access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
